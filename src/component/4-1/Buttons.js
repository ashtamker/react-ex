const Buttons = (props) => {
   return (
       <div>
           <button style={{fontWeight:'bold'}}>Important</button>
           <button>Not Important</button>
       </div>
   )

}


export default Buttons;